# Local deploy config (never commit — contains your server details)
.env.deploy
.env

# Ansible vault password file
.vault-pass

# Per-site configuration and secrets (machine-local, never commit)
sites/

# Ansible fact cache
/tmp/ansible_facts_cache/

# Ansible retry files
*.retry

# macOS
.DS_Store
